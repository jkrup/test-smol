// Check if Web3 is already injected
if (typeof web3 !== 'undefined') {
    web3 = new Web3(web3.currentProvider);
} else {
    // Specify default instance if no web3 instance provided
    web3 = new Web3(new Web3.providers.HttpProvider("http://localhost:8545"));
}

web3.eth.defaultAccount = web3.eth.accounts[0];

// Contract Address
var contractAddress = '0x1234...';

// ABI
var ABI = [...];

// Contract object
var contract = web3.eth.contract(ABI).at(contractAddress);

// Buy pixel function
function buyPixel(pixelId, color) {
    contract.buyPixel(pixelId, color, {from: web3.eth.defaultAccount}, function(error, result){
        if(!error)
            console.log(result);
        else
            console.error(error);
    });
}

// Change color function
function changeColor(pixelId, color) {
    contract.changeColor(pixelId, color, {from: web3.eth.defaultAccount}, function(error, result){
        if(!error)
            console.log(result);
        else
            console.error(error);
    });
}